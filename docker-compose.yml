version: '3.8'

services:
  dev:
    build:
      context: .
      target: dev
    image: desktop-overlay:dev
    container_name: desktop-overlay-dev
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - /run/user/${UID:-1000}/pulse:/run/user/1000/pulse
      - .:/app
    environment:
      - DISPLAY=${DISPLAY}
      - PULSE_SERVER=unix:/run/user/1000/pulse/native
    network_mode: "host"
    stdin_open: true
    tty: true
    command: /bin/bash
  app:
    build:
      context: .
      target: app
    image: desktop-overlay:v0.0.0
    container_name: desktop-overlay-app
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - /run/user/1000/pulse:/run/user/1000/pulse
    environment:
      - DISPLAY=${DISPLAY}
      - PULSE_SERVER=unix:/run/user/1000/pulse/native
    network_mode: "host"
